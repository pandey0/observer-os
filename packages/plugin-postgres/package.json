{
  "name": "@observer-os/plugin-postgres",
  "version": "1.1.0",
  "description": "Observer OS plugin — instruments node-postgres (pg) Pool and Client",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "prepack": "pnpm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "peerDependencies": {
    "pg": ">=8.0.0"
  },
  "dependencies": {
    "@observer-os/core": "workspace:*",
    "@observer-os/sdk": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "pg": "^8.13.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}

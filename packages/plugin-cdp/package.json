{
  "name": "@observer-os/plugin-cdp",
  "version": "1.1.0",
  "description": "Observer OS Chrome DevTools Protocol bridge via Puppeteer",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "prepack": "pnpm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "puppeteer-core": "^23.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}

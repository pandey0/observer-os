# @observer-os/mcp-server

## 1.1.0

### Minor Changes

- Initial public release of Observer OS — local-first runtime intelligence platform.

  Auto-instrument any Node.js app with zero code changes. Captures HTTP requests, SQL queries (PostgreSQL, MySQL), Redis commands, WebSocket messages, and console output. Exposes runtime graph to humans via Explorer UI and to AI agents via MCP server.

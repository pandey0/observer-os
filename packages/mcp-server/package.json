{
  "name": "@observer-os/mcp-server",
  "version": "1.1.0",
  "description": "Observer OS MCP Server — expose runtime intelligence to Claude",
  "files": [
    "dist"
  ],
  "type": "module",
  "bin": {
    "observer-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "prepack": "pnpm run build",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "zod": "^3.25.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
